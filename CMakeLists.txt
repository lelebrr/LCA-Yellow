# Este arquivo é usado para auxiliar o IntelliSense do VS Code e não afeta a compilação do PlatformIO.
cmake_minimum_required(VERSION 3.16)
project(LCA_Toolkit)

# Adicione aqui os diretórios de inclusão para que o IntelliSense encontre os headers.
# O PlatformIO geralmente cuida disso automaticamente, mas em caso de problemas,
# você pode adicionar manualmente os caminhos.
# Exemplo: include_directories(include src)

# Este arquivo pode ser deixado em branco na maioria dos casos,
# pois o plugin do PlatformIO no VS Code gerencia as configurações de compilação.
